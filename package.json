{
  "name": "scorekit",
  "version": "0.1.0",
  "private": true,
  "description": "Template-driven assessment platform for business diagnostics",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "test:watch": "pnpm --filter @scorekit/core test:watch",
    "convert-logos": "node scripts/convert-logos.mjs"
  },
  "devDependencies": {
    "sharp": "^0.33.5",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=22 <23"
  },
  "packageManager": "pnpm@10.18.3"
}
