/**
 * AI Capability Template
 *
 * Exports the complete template content. (Named export only — avoids colliding
 * with other templates' deprecated `content` alias in the barrel.)
 */

export { aiCapabilityContent } from "./content";
