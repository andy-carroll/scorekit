# Session 2026-06-25-1054

## Repo health
- verify: pass (`pnpm typecheck && pnpm test && pnpm build` — now typechecks both packages; 53 tests; build OK) | tree: clean | deploy: Vercel preview green on PR #9

## Review
- clean — independent fresh-eyes review of the cumulative `main...HEAD` diff: APPROVE, no blocking issues. Confirmed the CI step order is correct (pnpm before setup-node, required for `cache: pnpm`), version pins consistent, `--frozen-lockfile` safe, `engines` advisory-only (no hard-fail on local Node 26). One non-blocking finding raised — `pnpm typecheck` only covered `packages/core` — was **fixed in this session** rather than deferred (added `typecheck` script to `apps/web` + repaired two pre-existing `Lead` mock type errors).

## What was done
- **Closed #5 — pinned Node to 22 LTS.** Added `.nvmrc` (`22`) and `engines.node` (`>=22 <23`) so contributors (nvm), CI, and Vercel all resolve the same LTS, steering off the Node v25+ experimental global `localStorage` that destabilised Vitest.
- **Hardened CI** (`.github/workflows/ci.yml`): reads `node-version-file: .nvmrc`, added a `pnpm typecheck` step before tests, installs with `--frozen-lockfile`, and switched to `pnpm/action-setup@v4` (infers pnpm from `packageManager`) — fixing a latent `pnpm@8` vs `pnpm@10.18.3` mismatch. Reordered so pnpm sets up before Node (needed for `cache: pnpm`).
- **Made the typecheck gate meaningful across the monorepo**: added `"typecheck": "tsc --noEmit"` to `apps/web` (previously only `packages/core` had one, so CI's typecheck silently skipped the Next.js app) and fixed the two `Lead` mock errors that surfaced.
- Updated `docs/00-overview/WORKFLOW.md` CI description to match.

## Repo state
- Branch: `chore/pin-node-lts` → merged to `main` via PR #9
- Open issues: 4 (#3, #4, #6, #7) — all backlog/future work; #5 closed by this PR
- Open PRs: none after merge

## Next session — start here
Decide the direction on EPIC [#7](https://github.com/andy-carroll/scorekit/issues/7): confirm whether the richer business-case report engine is still wanted, or stay on the simpler MVP scoring path — this gates 8 deferred tickets. If not picking that up, the next concrete work is a template build ([#3](https://github.com/andy-carroll/scorekit/issues/3) COO chaos or [#4](https://github.com/andy-carroll/scorekit/issues/4) Recruitment), or [#6](https://github.com/andy-carroll/scorekit/issues/6) Report V2 UAT & polish.
