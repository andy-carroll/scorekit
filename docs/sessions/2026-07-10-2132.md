# Session 2026-07-10-2132

Two-repo session (scorekit + ax-agent-hub) plus the live Tally form and Altima Drive artifacts. This handoff lives in scorekit but covers both repos; ax-agent-hub has open work noted below and should get its own close.

## Repo health
- verify: pass (`pnpm typecheck && pnpm test && pnpm build` — 92 tests: 26 core + 66 web; build OK) | tree: clean | deploy: Vercel auto-deploys on merge to main

## Review
- clean — independent fresh-eyes review of scorekit PR #30 cumulative diff (`main...fix/ai-capability-v1.1-sync`): **no blocking findings.** Confirmed the quiz `required: false` gating change is a genuine bugfix and leaves ai-readiness byte-for-byte unchanged (ai-readiness sets no `required` field; `undefined === false` is false, so required questions still gate); scoring handles the uneven responsible-use pillar (3 questions, max 15) via per-pillar averages; free-text context questions are unscored and render in the profile section; tests assert the real 13-question / 2-2-2-2-3-2 / 5-option shape.
- 1 non-blocking finding fixed before merge: PDF profile subtitle "at the start of the assessment" → "during the assessment" (v1.1's free-text context questions sit at the end). 2 trivial notes not raised as issues: em-dashes in ai-capability.ts code comments (house rule targets output copy, comments exempt); whitespace-only required-text edge (pre-existing gating pattern).

## What was done
**The AI Capability Assessment moved from "template built" to "end-to-end pipeline working", across two tiers.**

- **Tally form KYRAMA → v1.1 (live, published, shared with the Altima AI programme leadership group).** Added the 5th "Leading" option to all questions; replaced the frequency Q4 with a prompt-construction craft question; added a third responsible-use question (output ownership/disclosure); added an "In your own words" section with a **required** recent-work free-text anchor and an optional frustration question; fixed the "Other" role option to expose a type-in; made demographics genuinely optional; added a thank-you page and changed the submit button from "See my results" to "Submit my answers" (managed pilot, not instant result). All em-dashes removed. 13 scored + 2 free-text + 2 demographic.
- **scorekit (PR #30, merged this close):** synced the `ai-capability` template to v1.1 (question set, scoring for the uneven pillar, tests 90→92), and fixed a real quiz bug where `required: false` was never honoured in Next-button gating. ai-readiness untouched.
- **ax-agent-hub — `ax-capability-report` Tier-2 skill:** render step JSON→PDF built and merged (#626); reference pass adding `voice.md` (the binding voice standard), v1.1 sync, and worked examples (#642, merged); QA polish (#643, merged) after an **independent skill-qa scored 28/30, PASS**; de-client refactor to a fully generic product skill (**PR #645, OPEN**). Full pipeline dry-run verified: synthetic v1.1 submission → narrative → designed PDF.
- **Voice + brand assets:** authored the capability-report voice standard (thinking model, structural + word blacklist, calibration rewrites) with Andy's founder-voice exemplar; extracted an Altima register from their Staff Handbook (client folder, word-choice-only layer); rebalanced the Altima overview doc to the AX design system (navy hero, pink as accent only) and produced a sample consultant report — both in `Clients/Altima/03-Delivery/AI Fluency and Capability Assessment/`.

**Architecture decision (Andy):** one generic `ax-capability-report` product skill, never per-client forks. Client tailoring is a runtime layer, never a fork: (a) a client register file supplied alongside `voice.md` (word choice only), (b) a client-branded PDF template in `assets/` via a future `--template` flag. Promotes to a plugin (recommend `ax-delivery`), not a client `.skill` archive.

## Repo state
- scorekit branch: `fix/ai-capability-v1.1-sync` → merged to main via PR #30 (this close). Issue #29 closed.
- scorekit open issues: #23 (EPIC Altima Tier-1 delivery), #26 deploy / #27 custom domain / #28 co-brand (per the strategy pivot these now serve the **public lead-magnet** deployment of the ScoreKit template, NOT Altima — Altima receives the Wren consultant report, not the self-serve tool), #6 Report V2 UAT, #7 EPIC business-case engine, #3 / #4 other templates. No open scorekit PRs after merge.
- **ax-agent-hub (separate repo, needs its own close):** #642, #643, #626 merged to main mid-session (skill-qa 28/30 served as the review gate for the skill content). **PR #645 (de-client) is OPEN, awaiting review + Gate 2 promote.** The Tier-2 skill is not yet promoted out of `skills/staging/`.

## Next session — start here
Review and merge ax-agent-hub **PR #645** (makes `ax-capability-report` fully client-agnostic), then run the **promote to the `ax-delivery` plugin** (Gate 2: it passed skill-qa at 28/30). That is the last gate before the Tier-2 consultant-report pipeline is production-usable. Once promoted, the pipeline is ready for the first real Altima submission: Tally (v1.1, live) → pull responses → run `ax-capability-report` → `render-report.mjs` → PDF to the Altima folder for human review before sending. Still outstanding from Andy: 2–3 founder-voice paragraphs are now supplied, but Toby-voice and any further exemplars remain open; Airtable destination (base/table) for Tally responses undecided (holding the native Tally→Airtable integration until then).
